import { Injectable } from '@angular/core';
import { Store, select } from '@ngrx/store';
import * as fromStore from '../<%= containerToCorePath %>/store/app.reducer';
import { EntitySelectorService } from '../<%= containerToCorePath %>/store/app.selectors';

import { Observable, of, combineLatest } from 'rxjs';
import { bufferTime, distinctUntilChanged, shareReplay, mergeMap, filter, switchMap, map } from 'rxjs/operators';

@Injectable({
  providedIn: 'root',
})
export class <%= classify(name) %>Selectors {
  constructor(
    private slRx: EntitySelectorService,
  ) { }

  cleanup(cId: string) {
    this.slRx.release(cId);
  }
}
