{
  "$schema": "http://json-schema.org/schema",
  "$id": "EntitySchema",
  "title": "Entity Schema",
  "type": "object",
  "description": "Creates a new entity in the default project.",
  "properties": {
    "index": {
      "type": "number",
      "default": 1
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the entity file, relative to the current workspace. Should typically be within /src/app/core/store.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
      }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "name": {
      "type": "string",
      "description": "The name of the entity.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the entity?"
    },
    "inDirectory": {
      "type": "boolean",
      "x-prompt": "Are you currently in the directory where you want to generate the entity? (typically directly in src/app/core/store)"
    }
  },
  "required": [
    "name"
  ]
}
