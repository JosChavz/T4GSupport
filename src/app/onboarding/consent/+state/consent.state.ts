import { Injectable } from '@angular/core';

import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root',
})
export class ConsentState {
  // --------------- LOCAL UI STATE ----------------------

  constructor(
  ) { }
}
